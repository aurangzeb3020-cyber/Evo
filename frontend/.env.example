# Evocative frontend – copy to .env and adjust (optional).
# Only public, non-secret values belong here. Never put API keys or secrets in VITE_ variables;
# they would be embedded in the client bundle and exposed.

# Base URL for the API (same-origin in production). Leave empty to use current origin.
VITE_API_BASE=
